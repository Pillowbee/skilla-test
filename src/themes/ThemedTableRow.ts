import { styled } from '@mui/material/styles'
import TableRow from '@mui/material/TableRow'

export const ThemedTableRow = styled(TableRow)({
  [`&.MuiTableRow-hover:hover`]: {
    backgroundColor: '#D4DFF32B',
  },
})